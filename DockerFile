# Use Node.js LTS
FROM node:18

# Set workdir
WORKDIR /usr/src/app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source
COPY . .

# Build TypeScript
RUN npm run build

# Expose port
EXPOSE 3001

# Run the app
CMD ["node", "dist/app.js"]
